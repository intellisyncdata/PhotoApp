<Page x:Class="DemoPhotoBooth.Pages.CameraPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DemoPhotoBooth.Pages"
      xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CameraPage" Background="White"
      Loaded="Page_Loaded">

    <Grid x:Name="MainGrid">
        <!--<svg:SvgViewbox Stretch="Fill" Grid.ColumnSpan="4" Grid.RowSpan="4">
            <svg:SvgCanvas Source="pack://application:,,,/Layouts/horizontal_screen.svg"/>
        </svg:SvgViewbox>-->
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="3.5*" />
            <RowDefinition Height="3.5*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*" />
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="0.5*" />
        </Grid.ColumnDefinitions>
        <ListBox x:Name="CameraListBox" Visibility="Hidden"/>
        <!-- Live View Area -->
        <Canvas  x:Name="Slider" Grid.Row="0"  Grid.Column="1" Grid.ColumnSpan="2"  Grid.RowSpan="4" >
            <Canvas.Background>
                <ImageBrush x:Name="LiveViewBrush" Stretch="Uniform"/>
            </Canvas.Background>
        </Canvas>
        <Viewbox Stretch="UniformToFill" Grid.RowSpan="4" Grid.ColumnSpan="4">
            <svg:SvgCanvas x:Name="MySvgCanvasLiveView"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"/>
        </Viewbox>
        <!-- Countdown Timer (Top-Right) -->
        <TextBlock Name="CountdownTimer"
                   FontSize="48"
                   FontWeight="Bold"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   Margin="20,20,20,0"
                   Foreground="Red"
                   TextAlignment="Center" 
                   Visibility="Hidden"
                   Grid.ColumnSpan="2"/>

        <!-- Remaining Photos Counter -->
        <TextBlock Name="PhotosLeftCounter"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Bottom"
                   Margin="20"
                   Foreground="Red"
                   Grid.Row="3" 
                   Grid.ColumnSpan="1"/>
    </Grid>
</Page>
