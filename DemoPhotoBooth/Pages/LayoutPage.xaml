<Page x:Class="DemoPhotoBooth.Pages.LayoutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
      xmlns:common="clr-namespace:DemoPhotoBooth.Common"
      mc:Ignorable="d"
      d:DesignHeight="800" d:DesignWidth="1100"
      Title="LayoutPage" Height="Auto" Width="Auto" >

    <!-- Định nghĩa tài nguyên -->
    <Page.Resources>
        <common:ResponsiveImageSizeConverter x:Key="ResponsiveImageSizeConverter"/>
        <common:ResponsiveImageHeightConverter x:Key="ResponsiveImageHeightConverter"/>
    </Page.Resources>

    <!-- Giao diện người dùng -->
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="pack://application:,,,/Layouts/main-background.png"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>
        <!-- Title -->
        <TextBlock Text="Chọn Khung Ảnh" 
                   FontSize="48" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center"
                   VerticalAlignment="Top"
                   Margin="0,20,0,0"
                   Foreground="#000000" Grid.Row="0"/>

        <!-- Layout Items -->
        <ItemsControl ItemsSource="{Binding Layouts}" Grid.Row="1">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <!-- Sử dụng UniformGrid để bố cục 3 cột -->
                    <UniformGrid Rows="2" Columns="3" HorizontalAlignment="Center" VerticalAlignment="Top" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="50,0,0,50" MouseLeftButtonDown="OnLayoutClick">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="5*"/>
                            <!-- Hàng chứa SVG -->
                            <RowDefinition Height="1*"/>
                            <!-- Hàng chứa giá -->
                        </Grid.RowDefinitions>

                        <Viewbox Grid.Row="0" Stretch="Uniform" 
         HorizontalAlignment="Center" 
         VerticalAlignment="Center" 
         Width="300" Height="300">
                            <Image Source="{Binding ImageUrl}" 
           Stretch="Uniform" 
           Margin="0"/>
                        </Viewbox>

                        <!-- Price (Di chuyển xuống dòng dưới cùng) -->
                        <TextBlock Grid.Row="1"
                           Text="{Binding Price, StringFormat='{}{0:N0} đ'}"
                           FontSize="22" 
                           FontWeight="Bold" 
                           Foreground="#d18c6a"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Top">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Margin" Value="0,5,0,0"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding FrameType}" Value="vertical">
                                            <Setter Property="Margin" Value="0,10,0,0"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                    </Grid>
                </DataTemplate>

            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Button Content="Quay lại"
        HorizontalAlignment="Right" 
        VerticalAlignment="Bottom"
        Margin="0,0,50,40" 
        Padding="10,5"
        Width="300"
        Height="80"
        Background="#f4c76e"
        FontWeight="Bold"
        FontSize="32" 
        Foreground="White"
        Click="NavigateHome_Click" Grid.Row="1">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" 
                    CornerRadius="35" 
                    BorderThickness="0">
                        <TextBlock Text="{TemplateBinding Content}" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           Foreground="White"
                           FontWeight="{TemplateBinding FontWeight}" />
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>


    </Grid>

</Page>
